import AdminSetup from './AdminSetup';

export default function AdminSignup() {
      // Safe onboarding: redirects full signup flow through Canteen & Owner Registration
      return <AdminSetup />;
}
